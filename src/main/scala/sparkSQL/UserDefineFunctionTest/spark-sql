1.spark-sql中的自定义函数
    spark-sql支持的自定义函数有udf和udaf
        udf函数支持的是多进一出，实现一些复杂的计算，将不同的行进行聚合，聚合为一行，将这一行数据输出
        输入形式是一行的多个数据，输出的是多个数据聚合后的结果．
        例如:输入的多行为多个学科的成绩，输出为这些学科成绩的总成绩
        是一行对应一个结果．

        udaf(user define aggregate functions)用户自定义的聚合函数.
        将一列的多个数据输入，得到一个具体的输出.
        例如:一列有很多的重复值，想要计算这列中不重复值的个数，可以自定义一个udaf函数进行处理
２.spark-sql中的schema指定
    可以逐个设置，可以通过读取一个文件，对文件进行处理后进行设置